import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  label: {
    position: 'absolute',
    fontFamily: 'Roboto-Regular',
    paddingBottom: 0,
    paddingLeft: 12,
    left: 0,
    right: 'auto',
    lineHeight: 20,
  }
});